# Example .env file for LLaMa-Herd frontend
#
# This file is used by Create React App to configure environment variables.
# Copy this file to .env in the root directory and customize it for your local development.
# These variables are embedded at build time.
#
# See: https://create-react-app.dev/docs/adding-custom-environment-variables/

# The base URL for the backend API.
# This should include the protocol and port, but not the /api suffix.
REACT_APP_API_BASE_URL=http://localhost:8000

# The base URL for the Ollama API.
# This should include the protocol and port.
REACT_APP_OLLAMA_BASE_URL=http://localhost:11434
